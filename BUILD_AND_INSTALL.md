# Сборка и установка приложения

## Требования

- Node.js 18+ и npm
- Все зависимости из package.json

## 1. Подготовка к сборке

### 1.1 Установка зависимостей

```bash
npm install
```

### 1.2 Настройка переменных окружения

Создайте файл `.env.production` в корне проекта:

```env
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
VITE_SUPABASE_PROJECT_ID=default
```

**Для локальной сети:** Замените `localhost` на IP-адрес сервера:
```env
VITE_SUPABASE_URL=http://192.168.1.100:54321
```

## 2. Сборка приложения

```bash
npm run build
```

После успешной сборки будет создана папка `dist/` с готовым приложением.

## 3. Упаковка для распространения

### 3.1 Создание архива

```bash
# Windows
tar -czf medical-training-app.tar.gz dist/

# Linux/Mac
tar -czf medical-training-app.tar.gz dist/
```

### 3.2 Что включить в пакет

Создайте папку `medical-training-package/` со следующей структурой:

```
medical-training-package/
├── dist/                          # Собранное приложение
├── server/
│   └── serve.js                   # HTTP сервер
├── node_modules.tar.gz            # Зависимости (опционально)
├── package.json
├── INSTALL.md                     # Инструкция по установке
└── start.bat / start.sh           # Скрипты запуска
```

## 4. Создание HTTP сервера

### 4.1 Установка serve

```bash
npm install -g serve
```

### 4.2 Создание простого Node.js сервера

Создайте файл `server/serve.js`:

```javascript
const express = require('express');
const path = require('path');
const app = express();
const PORT = process.env.PORT || 3000;

// Раздача статических файлов
app.use(express.static(path.join(__dirname, '../dist')));

// Все маршруты направляем на index.html (для SPA)
app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, '../dist/index.html'));
});

app.listen(PORT, '0.0.0.0', () => {
  console.log(`🚀 Приложение запущено на http://localhost:${PORT}`);
  console.log(`📡 Доступно в сети по адресу http://<IP-адрес>:${PORT}`);
});
```

### 4.3 Минимальный package.json для сервера

```json
{
  "name": "medical-training-server",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/serve.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
```

## 5. Скрипты запуска

### 5.1 Windows (start.bat)

```batch
@echo off
echo Запуск приложения Medical Training...
echo.

REM Проверка Node.js
where node >nul 2>nul
if %errorlevel% neq 0 (
    echo [ОШИБКА] Node.js не установлен!
    echo Установите Node.js с https://nodejs.org/
    pause
    exit /b 1
)

REM Установка зависимостей если нужно
if not exist "node_modules" (
    echo Установка зависимостей...
    call npm install
)

REM Запуск сервера
echo Запуск сервера...
echo Приложение будет доступно по адресу: http://localhost:3000
echo.
npm start
```

### 5.2 Linux/Mac (start.sh)

```bash
#!/bin/bash

echo "Запуск приложения Medical Training..."
echo ""

# Проверка Node.js
if ! command -v node &> /dev/null; then
    echo "[ОШИБКА] Node.js не установлен!"
    echo "Установите Node.js с https://nodejs.org/"
    exit 1
fi

# Установка зависимостей если нужно
if [ ! -d "node_modules" ]; then
    echo "Установка зависимостей..."
    npm install
fi

# Запуск сервера
echo "Запуск сервера..."
echo "Приложение будет доступно по адресу: http://localhost:3000"
echo ""
npm start
```

Сделайте скрипт исполняемым:
```bash
chmod +x start.sh
```

## 6. Инструкция по установке (INSTALL.md)

```markdown
# Установка Medical Training Platform

## Быстрый старт

### Windows
1. Распакуйте архив
2. Дважды щёлкните `start.bat`
3. Откройте браузер: http://localhost:3000

### Linux/Mac
1. Распакуйте архив
2. Откройте терминал в папке приложения
3. Запустите: `./start.sh`
4. Откройте браузер: http://localhost:3000

## Требования

- Node.js 18 или новее
- Запущенный локальный Supabase (см. DEPLOYMENT_OFFLINE.md)

## Доступ из локальной сети

Приложение автоматически доступно по IP-адресу компьютера:
- http://192.168.1.100:3000 (замените IP на ваш)

## Остановка

- Нажмите Ctrl+C в окне терминала

## Решение проблем

### Порт 3000 уже занят
Измените порт в server/serve.js или установите переменную:
```bash
PORT=8080 npm start
```

### Ошибки подключения к базе данных
Убедитесь что Supabase запущен:
```bash
npx supabase status
```
```

## 7. Альтернатива: Использование serve

Простейший вариант без Node.js сервера:

```bash
# Установите serve глобально
npm install -g serve

# Запуск
serve -s dist -p 3000
```

## 8. Создание полного пакета

### 8.1 Скрипт упаковки (package.sh)

```bash
#!/bin/bash

echo "Создание пакета для распространения..."

# Сборка приложения
echo "1. Сборка приложения..."
npm run build

# Создание структуры
echo "2. Создание структуры пакета..."
mkdir -p medical-training-package/server
mkdir -p medical-training-package/dist

# Копирование файлов
echo "3. Копирование файлов..."
cp -r dist/* medical-training-package/dist/
cp server/serve.js medical-training-package/server/
cp package.json medical-training-package/
cp start.bat medical-training-package/ 2>/dev/null || true
cp start.sh medical-training-package/ 2>/dev/null || true
cp INSTALL.md medical-training-package/
cp DEPLOYMENT_OFFLINE.md medical-training-package/

# Упаковка node_modules (опционально)
echo "4. Упаковка зависимостей..."
tar -czf medical-training-package/node_modules.tar.gz node_modules/

# Создание финального архива
echo "5. Создание архива..."
tar -czf medical-training-app-complete.tar.gz medical-training-package/

echo ""
echo "✅ Пакет готов: medical-training-app-complete.tar.gz"
echo ""
echo "Содержимое:"
echo "- Собранное приложение (dist/)"
echo "- HTTP сервер (server/)"
echo "- Зависимости Node.js (node_modules.tar.gz)"
echo "- Скрипты запуска"
echo "- Инструкции по установке"
```

## 9. Оптимизация для offline использования

### 9.1 Кэширование ассетов

Vite автоматически создает оптимизированную сборку с:
- Минифицированным JS/CSS
- Хэшами файлов для кэширования
- Разделением кода (code splitting)

### 9.2 Service Worker (опционально)

Для полностью offline приложения можно добавить Service Worker:

```bash
npm install -D vite-plugin-pwa
```

Обновите `vite.config.ts`:
```typescript
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg,jpg}']
      }
    })
  ]
})
```

## 10. Тестирование сборки

```bash
# Локальное тестирование
npm run build
npm run preview

# Или с помощью serve
serve -s dist
```

## 11. Чек-лист перед распространением

- [ ] Приложение собрано: `npm run build`
- [ ] Проверены переменные окружения (.env.production)
- [ ] Создан HTTP сервер (serve.js)
- [ ] Созданы скрипты запуска (start.bat, start.sh)
- [ ] Написана инструкция INSTALL.md
- [ ] Упакованы зависимости (node_modules.tar.gz)
- [ ] Протестирован запуск на чистой системе
- [ ] Проверена работа в локальной сети
- [ ] Создан финальный архив

## 12. Размер пакета

Ориентировочные размеры:
- dist/: ~5-10 MB (в зависимости от ассетов)
- node_modules.tar.gz: ~150-200 MB
- Полный пакет: ~160-210 MB

## 13. Системные требования

### Минимальные
- ОС: Windows 10/11, Ubuntu 20.04+, macOS 11+
- RAM: 2 GB
- Диск: 500 MB свободного места
- Node.js: 18.0+

### Рекомендуемые
- RAM: 4 GB+
- Диск: 1 GB свободного места
- Node.js: 20.0+

## Поддержка

Для вопросов и проблем см. документацию DEPLOYMENT_OFFLINE.md
